{
  "watch": ["src", "prisma"],
  "ext": "ts,js,json",
  "ignore": ["node_modules", "dist"],
  "exec": "ts-node-dev --respawn --transpile-only src/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
